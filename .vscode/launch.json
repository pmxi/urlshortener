{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: URL Shortener Dev Server",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/dev_server.py",
            "python": "${workspaceFolder}/.venv/bin/python",
            "console": "integratedTerminal",
            "justMyCode": false,
            "env": {
                "PYTHONPATH": "${workspaceFolder}/src"
            }
        }
    ]
}
